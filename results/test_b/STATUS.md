# Synthetic Test B status

Status: **pending upstream broadened-signal BLISS script and definitive export**.

The preregistration, adapter, union, inference, label-reveal, evaluation, and
artifact-freezing code is present under `stages/real_pair/`. No Test-B
performance result is included, and no placeholder metrics should be added.

When the locked run is complete, append the run identifier, date, source and
checkpoint hashes, exclusions, and supported claim while retaining this
history.
