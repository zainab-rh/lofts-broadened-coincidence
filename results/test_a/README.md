# Synthetic Test-A results

This directory is reserved for the frozen Stage-4 held-out synthetic result,
including its manifest, pair-level score export, paired-bootstrap comparison,
and checkpoint hashes. Keep training outputs in a named subdirectory and
separate them from the locked evaluation tables.
